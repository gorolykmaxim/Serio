import React from "react";
import {CircularProgress as Progress} from "@material-ui/core";

export default function CircularProgress(props) {
    return <Progress size={140} style={{color: '#FB8C00'}}/>;
}
